# Telemetry

Launchframe does not collect product telemetry by default.

The scaffolded workspace templates include telemetry and privacy placeholders so each launched product can state its own behavior accurately.

