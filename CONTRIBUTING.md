# Contributing

Launchframe is early. Contributions should make launches more repeatable, auditable, or easier to verify.

Before opening a PR:

1. Run `npm run check`.
2. Run `npm run smoke`.
3. Keep templates generic; do not add product-specific claims to reusable templates.
4. Keep examples under `examples/`.

