# <Product> Telemetry And Data

Last updated: `<date>`

This document states what `<Product>` does and does not send from user environments.

> Launch gate: verify this against source code, package defaults, docs, analytics configuration, and network behavior before publication.

## Summary

- Telemetry default: `<off by default / on by default / none>`
- Hosted analytics: `<provider / none>`
- Error reporting: `<provider / none>`
- Disable command or setting: `<command / config / env var / not applicable>`

## CLI / Local Tooling

By default, the CLI:

- Sends: `<none / event names / version / OS / command name / other>`
- Does not send: `<source code / file contents / prompts / secrets / issue data / other>`
- Stores locally: `<config path / cache path / logs / none>`

## Website Analytics

The website uses:

- Analytics provider:
- Events collected:
- Cookies used:
- Retention:

## Error Reporting

Error reporting:

- Provider:
- Data included:
- Data excluded:
- How to opt out:

## Verification

Commands or checks used to verify:

```sh
<network/log/source verification commands>
```

Reviewer:

Date:

